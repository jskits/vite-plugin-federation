/**
 * Keep formatting intentionally small and predictable across the monorepo.
 * ESLint handles correctness; Prettier handles layout.
 */
export default {
  printWidth: 100,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
};
